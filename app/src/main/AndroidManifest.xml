<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pesmoce.foss.codefiesta">

        <application
            android:allowBackup="true"
            android:icon="@mipmap/cf_logo"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            >
            <activity android:name=".MainActivity">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />

                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>
            <activity android:name=".Main2Activity" />
            <activity android:name=".Main3Activity" />
            <activity android:name=".Main4Activity" />
            <activity android:name=".Main5Activity" />
            <activity android:name=".maze_coders" />
            <activity android:name=".allied" />
            <activity android:name=".frenzy" />
            <activity android:name=".sponsors" />
            <activity android:name=".prev_spo"></activity>
        </application>

    </manifest>
